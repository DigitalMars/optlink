		TITLE	LIBENTRY - Copyright (c) 1994 SLR Systems

		INCLUDE	MACROS

		PUBLIC	LIBENTRY


		.DATA

	SOFT	EXTD	HINSTANCE


		.CODE	ROOT_TEXT

LIBENTRY	PROC
		;
		;CALLED FROM WINDOWS WHEN DLL IS ATTACHED BY A PROCESS, AND
		;WHEN DETACHED BY A PROCESS
		;
		;RETURN EAX!=0 FOR SUCCESS
		;
		MOV	ECX,[ESP+4]	;HINSTANCE
;		MOV	EDX,[ESP+8]	;REASON
		OR	AL,1	;I DON'T CARE
		MOV	HINSTANCE,ECX

		RET	0CH

LIBENTRY	ENDP


		END	LIBENTRY

